<div>
  <% @students.each do |student| %>
    <div><%= link_to student.first_name + " " + student.last_name, student_path(student) %></div>
    <div><%= link_to "Toggle Active Status", activate_student_path(student)  %></div>
  <% end %>
</div>